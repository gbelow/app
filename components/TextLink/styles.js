import {StyleSheet} from 'react-native';

import theme from '../../const/theme';

const styles = StyleSheet.create({
  text: {
    color: theme.blue,
    fontSize: theme.smallFontSize,
    marginVertical: theme.gutter,
  },
});

export default styles;
